/*
import 'file:///C:/Users/Noa%20Elishmereni/Desktop/third%20year%20first%20semester/CS/FinalProject/cookids/lib/logIn/loginScreen.dart';
import 'package:cookids/afterCooking/reviewPage.dart';
import 'package:cookids/homePage/bodyLevelPage.dart';
import 'package:cookids/logIn/logOrSign.dart';
import 'package:cookids/main.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:cookids/constants.dart';
import 'package:cookids/beforeCooking/cookingPage.dart';

class BodyCookingPage extends StatelessWidget {
  //final pruduct pruduct;

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SingleChildScrollView(
        child: Column(
          children: <Widget>[
            SizedBox(
              height: size.height,
              child: Stack(
                children: <Widget>[
                  Container(
                      margin: EdgeInsets.only(
                          top: size.height * 0.48, left: size.width * 0.05),
                      height: 250,
                      width: 370,
                      decoration: BoxDecoration(
                        color: appbarColor,
                        borderRadius: BorderRadius.circular(16),
                      )),
                  Padding(
                      padding: const EdgeInsets.symmetric(horizontal: 20),
                      child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: <Widget>[
                            const SizedBox(height: 10),
                            Text("Banana",
                                style: Theme.of(context)
                                    .textTheme
                                    .headline4
                                    .copyWith(
                                    color: Colors.black,
                                    fontWeight: FontWeight.bold)),
                            const SizedBox(height: 15),
                            Row(
                              children: <Widget>[
                                Image.asset("images/banana.jpg", scale: 1.4),
                                Expanded(
                                  // ignore: deprecated_member_use
                                    child: FlatButton(
                                      onPressed: () {},
                                      padding: EdgeInsets.all(0.2),
                                      child: Image.asset(
                                        "images/voiceToText.png",
                                        scale: 1,
                                      ),
                                    ))
                              ],
                            ),
                            const SizedBox(height: 280),
                            Container(
                              height: 280,
                              child: ,
                            ),
                            // ignore: deprecated_member_use
                            Row(
                              children: <Widget>[
                                Expanded(
                                  // ignore: deprecated_member_use
                                  child: FlatButton(
                                    shape: RoundedRectangleBorder(
                                        borderRadius: BorderRadius.circular(10)),
                                    color: Colors.green,
                                    onPressed: () {
                                      Navigator.push(
                                        context,
                                        MaterialPageRoute(
                                          builder: (context) => ReviewPage(),
                                        ),
                                      );
                                    },
                                    child: Text("Next",
                                        style: TextStyle(
                                            fontSize: 20,
                                            fontWeight: FontWeight.bold,
                                            color: Colors.white)),
                                  ),
                                )
                              ],
                            ),
                          ])),
                ],
              ),
            )

            */
/*
            IconButton(
              icon: const Icon(Icons.arrow_back),
              onPressed: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(
                    builder: (context) {
                      return BodyLevelPage();
                    },
                  ),
                );
              },
            ),
            const SizedBox(height: 100),

            Container(
                padding: EdgeInsets.all(4),
                *//*
 */
/*    height: 180,
                width: 160,*//*
 */
/*
                decoration: BoxDecoration(
                  color: kPrimaryLightColor,
                  borderRadius: BorderRadius.circular(16),
                ),
                child: Image.asset("images/open.png")),
            const SizedBox(height: 50),
            ClipRRect(
                borderRadius: BorderRadius.circular(20),
                // ignore: deprecated_member_use
                child: FlatButton(
                  color: Colors.black,
                  onPressed: () {
                    Navigator.push(
                      context,
                      MaterialPageRoute(
                        builder: (context) { },
                      ),
                    );
                  },
                  child: Text("lets pick up the ingredients!",
                      style: TextStyle(
                          fontSize: 20,
                          fontWeight: FontWeight.bold,
                          color: kPrimaryLightColor)),
                )),*//*

          ],
        ));
  }
}
*/
